alocal availableGames = {
  [9791603388] = {
    ["Name"] = "Underground Wars 2.0",
    ["Function"] = function()
      loadstring(game:HttpGet("https://raw.githubusercontent.com/UnknownDev125/Scriptorium-Hub/main/Underground%20Wars%202.0"))()
    end
  }
}

if availableGames[game.PlaceId] then
  -- Using pcall to handle potential errors
  local success, error = pcall(availableGames[game.PlaceId]["Function"])
  if success then
    -- Function executed without errors
  else
    warn("Error running function:", error) -- Log the error message
  end
else
  game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "Notification",
    Text = "Game Is Not Supported!",
    Duration = 5
  })
end
